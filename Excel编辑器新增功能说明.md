# 在线 Excel 新增功能说明（第一优先级）

本文档说明本次在在线 Excel 页面新增的按钮功能、使用方式和快捷键，面向业务用户和实施人员。

## 1. 功能总览

本次新增能力主要分为 5 类：

1. 排序能力
2. 筛选能力
3. 公式栏能力
4. 复制粘贴增强
5. 快捷键支持

页面上对应新增/增强的区域：

- `样式工具栏`：新增排序、筛选、快捷键入口
- `公式栏`：新增单元格地址显示、公式输入与填充
- `表格头部`：筛选开启后支持列筛选按钮

## 2. 按钮功能与使用说明

## 2.1 排序相关

### 2.1.1 `升序`

- 功能：对当前选区按第一列执行升序排序。
- 使用步骤：
  1. 先框选要排序的数据区域。
  2. 点击 `升序`。
- 说明：默认按“有表头”处理（首行为表头，不参与排序）。可通过 `有表头` 开关调整。

### 2.1.2 `降序`

- 功能：对当前选区按第一列执行降序排序。
- 使用步骤：
  1. 框选排序区域。
  2. 点击 `降序`。

### 2.1.3 `有表头` 开关

- 功能：控制排序时是否将首行当作表头。
- 规则：
  - 开启：首行不参与排序，仅下面数据排序。
  - 关闭：整个选区全部参与排序。

### 2.1.4 `多列排序`

- 功能：对同一选区按多个列条件排序（主排序列、次排序列等）。
- 使用步骤：
  1. 选中数据区域。
  2. 点击 `多列排序`。
  3. 在弹窗中设置排序规则（列 + 方向）。
  4. 点击 `开始排序`。
- 适用场景：先按“部门”排序，再按“姓名”排序。

## 2.2 筛选相关

### 2.2.1 `开启筛选`

- 功能：基于当前选区开启自动筛选。
- 使用步骤：
  1. 选择包含表头的数据区域（至少 1 行表头 + 1 行数据）。
  2. 点击 `开启筛选`。
- 结果：表头单元格会出现列筛选入口。

### 2.2.2 `清除筛选`

- 功能：清除当前工作表筛选配置，恢复全部显示。
- 使用步骤：直接点击 `清除筛选`。

### 2.2.3 `列筛选`

- 功能：对某一列进行值筛选（可多选值，支持关键字过滤候选项）。
- 使用步骤：
  1. 先开启筛选。
  2. 将选中单元格移动到目标列。
  3. 点击 `列筛选`（或点击表头小筛选按钮）。
  4. 选择需要保留的值，点击 `应用筛选`。
- 支持：`全选`、`全不选`、关键字过滤候选值。

## 2.3 公式栏相关

### 2.3.1 公式输入框

- 功能：编辑当前活动单元格内容，可输入普通值或公式。
- 公式写法：以 `=` 开头，例如 `=SUM(A1:A10)`、`=A1+B1`。

### 2.3.2 `应用`

- 功能：将公式栏当前内容写入当前活动单元格。
- 使用步骤：
  1. 单击一个单元格。
  2. 在公式栏输入内容。
  3. 点击 `应用`。

### 2.3.3 `填充选区`

- 功能：将公式栏内容批量写入当前选区所有单元格。
- 使用步骤：
  1. 选中一个区域。
  2. 在公式栏输入内容。
  3. 点击 `填充选区`。
- 注意：若目标单元格存在下拉校验，非法值会被跳过。

## 2.4 复制粘贴增强（按钮仍为“复制/剪切/粘贴”）

### 2.4.1 `复制`

- 功能：复制选区内容，内部复制可保留公式和样式。

### 2.4.2 `剪切`

- 功能：剪切选区内容，并写入剪贴板。

### 2.4.3 `粘贴`

- 功能：粘贴到当前选区起点。
- 增强点：
  - 内部复制粘贴保留公式（不是仅保留显示值）。
  - 保留单元格样式（如字体颜色、加粗等）。
  - 对下拉校验单元格执行合法性检查，非法值自动跳过。

## 2.5 `快捷键` 按钮

- 功能：打开快捷键帮助弹窗。
- 使用方式：点击 `快捷键` 即可查看当前支持列表。

## 3. 快捷键清单

当前支持以下快捷键：

- `Ctrl + S`：保存当前工作簿
- `Ctrl + Z / Ctrl + Y`：撤销 / 重做
- `Ctrl + C / Ctrl + X / Ctrl + V`：复制 / 剪切 / 粘贴
- `Ctrl + Shift + L`：开启/关闭自动筛选
- `Alt + S`：打开多列排序面板
- `Alt + Shift + ↑ / Alt + Shift + ↓`：当前选区升序 / 降序
- `Alt + ↓`：打开当前单元格下拉列表（下拉校验单元格）
- `F2`：聚焦当前单元格编辑
- `Ctrl + Enter`：将公式栏内容填充到当前选区
- `Ctrl + F / Ctrl + H`：打开查找/替换

## 4. 常见使用场景示例

## 4.1 场景 A：先筛选再排序

1. 框选数据区域并点击 `开启筛选`。
2. 使用 `列筛选` 过滤目标数据。
3. 再点击 `多列排序` 做多字段排序。

## 4.2 场景 B：批量填充值

1. 选中目标区域。
2. 在公式栏输入固定值或公式。
3. 点击 `填充选区`（或使用 `Ctrl + Enter`）。

## 4.3 场景 C：带样式复制

1. 在源区域执行 `复制`。
2. 选择目标起始单元格。
3. 执行 `粘贴`，公式和样式会一起带过去。

## 5. 注意事项

- 排序暂不支持包含合并单元格的区域。
- 筛选需要有表头行，且至少有一行数据。
- 下拉校验列在批量粘贴时会自动拦截非法值，避免脏数据写入。
- 若快捷键无效，请先点击表格区域确保焦点在编辑页内。

## 6. 变更范围（便于开发排查）

主要页面文件：

- `src/views/tool/excel-editor/index.vue`

如需我再补一版“面向最终用户的简化操作手册（带截图占位）”，可以在此文档基础上再出一版精简版。
